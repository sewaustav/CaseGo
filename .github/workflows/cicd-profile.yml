name: CI/CD - Profile (Go)

on:
  push:
    branches: [main, dev, 'feature/**', 'feat/**']
    paths:
      - 'Profile/**'
      - '.github/workflows/cicd-profile.yml'
  pull_request:
    branches: [main, dev, 'feature/**', 'feat/**']
    paths:
      - 'Profile/**'
      - '.github/workflows/cicd-profile.yml'

jobs:
  test:
    name: Test & Build
    runs-on: ubuntu-latest
    # тесты — гейт на PR и на push в dev; на push в main пропускаем (PR уже проверил)
    if: github.event_name == 'pull_request' || github.ref != 'refs/heads/main'

    defaults:
      run:
        working-directory: Profile

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: Profile/go.mod
          cache-dependency-path: Profile/go.sum

      - name: Configure git for private modules
        run: git config --global url."https://${{ secrets.GH_PAT }}@github.com/".insteadOf "https://github.com/"

      - name: Download dependencies
        run: go mod download

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test ./... -race -coverprofile=coverage.out

      - name: Build
        run: go build ./cmd/caseGo/...

  docker:
    name: Docker Build & Push
    runs-on: ubuntu-latest
    needs: test
    if: >-
      always() &&
      (needs.test.result == 'success' || needs.test.result == 'skipped') &&
      (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/dev') &&
      github.event_name == 'push'
    permissions:
      contents: read
      packages: write

    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Set image tags
        id: tags
        run: |
          IMAGE=ghcr.io/${{ github.repository_owner }}/casego-profile
          # монотонная метка: github.run_number сбрасывается при переименовании
          # workflow, из-за чего numerical ImagePolicy залипает на старом теге
          SEQ=$(date +%s)
          if [ "${{ github.ref_name }}" = "main" ]; then
            # prod: main-теги подхватывает Flux image automation в k8s
            echo "tags=$IMAGE:main-$SEQ-${{ github.sha }}" >> "$GITHUB_OUTPUT"
          else
            # dev: :latest тянет docker compose на VPS
            echo "tags=$IMAGE:latest,$IMAGE:dev-$SEQ-${{ github.sha }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./Profile
          push: true
          tags: ${{ steps.tags.outputs.tags }}
          build-args: |
            SERVICE_NAME=caseGo
            GITHUB_TOKEN=${{ secrets.GH_PAT }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy Profile (dev VPS)
    runs-on: ubuntu-latest
    needs: docker
    if: github.ref == 'refs/heads/dev' && github.event_name == 'push'

    steps:
      - name: Deploy via SSH
        uses: appleboy/ssh-action@v1
        env:
          AUTH_PUBLIC_KEY: ${{ secrets.AUTH_PUBLIC_KEY }}
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: maks
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          envs: AUTH_PUBLIC_KEY
          script: |
            cd ~/casego/backend
            git fetch origin dev && git checkout -B dev origin/dev
            python3 -c 'import re,os; k=os.environ["AUTH_PUBLIC_KEY"]; e=k.strip().replace(chr(10),r"\n")+r"\n"; v="PUBLIC_KEY=\""+e+"\""; p="/home/maks/casego/backend/Profile/.env"; c=open(p).read(); open(p,"w").write(re.sub(r"PUBLIC_KEY=(\x22[^\x22]*\x22|[^\n]*)",lambda m:v,c,flags=re.DOTALL)); print("Updated",p)'
            echo ${{ secrets.GH_PAT }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
            docker compose pull app
            docker compose up -d --no-deps app
